
vendor(name, val)
	-webkit-{name}: val
	-moz-{name}: val
	-ms-{name}: val
	-o-{name}: val
	{name}: val

.dm-flex
	display: -webkit-flex
	display: -ms-flex
	display: flex

.dm-flex-wrap
	vendor(flex-wrap, wrap)

.dm-flex-no-wrap
	vendor(flex-wrap, nowrap)

.dm-flex-grow-absolute
	vendor(flex-grow, 1)
	vendor(flex-basis, 0)
